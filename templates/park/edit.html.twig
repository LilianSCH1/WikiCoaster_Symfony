{% extends 'base.html.twig' %}

{% block title %}Modifier le parc{% endblock %}

{% block body %}
    <h1>Modifier le parc</h1>

    {{ include('park/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_park_index') }}" class="btn btn-outline btn-sm">Retour à la liste</a>

    {{ include('park/_delete_form.html.twig') }}
{% endblock %}
